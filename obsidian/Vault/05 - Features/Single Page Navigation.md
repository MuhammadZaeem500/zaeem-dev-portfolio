# Single Page Navigation

Hash-based navigation connects the fixed navbar and footer links to
semantic page sections. Smooth scrolling is enabled globally through
`html { scroll-behavior: smooth; }`. Keep anchor IDs consistent and
unique.
