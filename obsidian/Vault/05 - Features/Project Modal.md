# Project Modal

Project cards set `selectedProject`; AnimatePresence mounts an overlay
modal; backdrop and close-button interactions dismiss it; ProjectDetails
renders implementation and stack information by project id. Future
accessibility work should add dialog semantics, focus trapping and
Escape-key closing.
