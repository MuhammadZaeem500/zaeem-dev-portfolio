# Animations

Framer Motion powers scroll-entry transitions, staggered skills,
project-card motion and modal transitions. `viewport={{ once: false }}`
on many sections means animations replay when content re-enters the
viewport; Experience uses `once: true`.
