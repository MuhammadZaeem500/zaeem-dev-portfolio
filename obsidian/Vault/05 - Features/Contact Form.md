# Contact Form

The form collects name, email, subject and message, submits with
EmailJS, shows loading text, resets after success and displays feedback.
The repository also contains a separate POST API route that is not
currently used by the form.
