# Responsive Design

The site uses Tailwind responsive prefixes such as `sm`, `md`, `lg` and
`xl`. Major patterns include mobile-first stacked layouts, desktop
grids/flex rows, responsive typography, mobile navigation and a desktop
alternating experience timeline.
